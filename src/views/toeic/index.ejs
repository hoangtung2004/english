<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Danh sách đề thi TOEIC</title>
  <link rel="icon" href="/images/icon.png" type="image/png">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>
<style>
    .home-feature {
        border: 1px solid #e9ecef;
        border-radius: 1rem;
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        display: flex;
        flex-direction: column;
    }
    .home-feature:hover {
        transform: translateY(-5px);
        box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }
    .card-body {
        flex-grow: 1;
        display: flex;
        flex-direction: column;
    }
    .card-title {
        font-size: 1.25rem;
    }
    .card-text {
        flex-grow: 1;
    }
</style>
<body>
  <%- include('../partials/header') %>
  <main class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h2">Luyện thi TOEIC</h1>
    </div>

    <div class="row">
      <% tests.forEach(test => { %>
        <div class="col-md-6 col-lg-4 mb-4">
            <div class="card home-feature h-100">
                <div class="card-body">
                    <h5 class="card-title"><%= test.title %></h5>
                    <p class="card-text text-muted"><%= test.description || 'Bài kiểm tra tổng hợp các kỹ năng.' %></p>
                    <div class="mt-auto">
                        <small class="d-block text-muted mb-2">Thời lượng: <%= test.duration / 60 %> phút</small>
                        <a href="/toeic/select-parts?testId=<%= test._id %>" class="btn btn-primary w-100">Chi tiết</a>
                    </div>
                </div>
            </div>
        </div>
      <% }); %>
    </div>
  </main>
  <%- include('../partials/footer') %>
</body>
</html> 