<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Sửa từ vựng</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/header') %>
  <main class="container my-5">
    <h1 class="mb-4">Sửa từ vựng cho chủ đề: <%= topic.name %></h1>
    <form method="post" action="/admin/vocabulary-sets/<%= set._id %>/topics/<%= topic._id %>/words/<%= word._id %>/edit">
      <div class="mb-3">
        <label class="form-label">Từ</label>
        <input type="text" name="word" class="form-control" value="<%= word.word %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Nghĩa</label>
        <input type="text" name="meaning" class="form-control" value="<%= word.meaning %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Phát âm</label>
        <input type="text" name="pronunciation" class="form-control" value="<%= word.pronunciation %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Ví dụ</label>
        <input type="text" name="example" class="form-control" value="<%= word.example %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Hình ảnh (URL)</label>
        <input type="text" name="image" class="form-control" value="<%= word.image %>">
      </div>
      <div class="mb-3">
        <label class="form-label">Loại</label>
        <select name="category" class="form-select" required>
          <option value="basic" <%= word.category === 'basic' ? 'selected' : '' %>>Basic</option>
          <option value="intermediate" <%= word.category === 'intermediate' ? 'selected' : '' %>>Intermediate</option>
          <option value="advanced" <%= word.category === 'advanced' ? 'selected' : '' %>>Advanced</option>
        </select>
      </div>
      <div class="mb-3">
        <label class="form-label">Chủ đề (topic)</label>
        <input type="text" name="topic" class="form-control" value="<%= word.topic %>" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Độ khó</label>
        <input type="number" name="difficulty" class="form-control" min="1" max="5" value="<%= word.difficulty %>" required>
      </div>
      <button type="submit" class="btn btn-primary">Lưu thay đổi</button>
      <a href="/admin/vocabulary-sets/<%= set._id %>/topics/<%= topic._id %>/edit" class="btn btn-secondary">Hủy</a>
    </form>
  </main>
  <%- include('../partials/footer') %>
</body>
</html> 