<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Thêm chủ đề từ vựng</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/header') %>
  <main class="container my-5">
    <h1 class="mb-4">Thêm chủ đề cho bộ: <%= set.name %></h1>
    <form method="post" action="/admin/vocabulary-sets/<%= set._id %>/topics/create">
      <div class="mb-3">
        <label class="form-label">Tên chủ đề</label>
        <input type="text" name="name" class="form-control" required>
      </div>
      <div class="mb-3">
        <label class="form-label">Mô tả</label>
        <textarea name="description" class="form-control" rows="2"></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Lưu</button>
      <a href="/admin/vocabulary-sets/<%= set._id %>/topics" class="btn btn-secondary">Hủy</a>
    </form>
  </main>
  <%- include('../partials/footer') %>
</body>
</html> 