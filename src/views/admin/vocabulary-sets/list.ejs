<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/images/icon.png" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Quản lý bộ từ vựng</h1>
        <a href="/admin/vocabulary-sets/create" class="btn btn-success mb-3">+ Thêm bộ từ vựng</a>
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Tên bộ</th>
                    <th>Mô tả</th>
                    <th>Icon</th>
                    <th>Màu</th>
                    <th>Trạng thái</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                <% sets.forEach(set => { %>
                    <% const cardStyle = `background-color:${idx === 0 ? set.color : '#f6f6f6'}; color:#222;`; %>
                    <tr>
                        <td><%= set.name %></td>
                        <td><%= set.description %></td>
                        <td><%= set.icon %></td>
                        <td><span style="<%= cardStyle %>display:inline-block;width:24px;height:24px;border-radius:4px;"></span></td>
                        <td><%= set.status %></td>
                        <td>
                            <a href="/admin/vocabulary-sets/edit/<%= set._id %>" class="btn btn-warning btn-sm">Sửa</a>
                            <form action="/admin/vocabulary-sets/delete/<%= set._id %>" method="POST" style="display:inline-block" onsubmit="return confirm('Xác nhận xóa?')">
                                <button type="submit" class="btn btn-danger btn-sm">Xóa</button>
                            </form>
                        </td>
                    </tr>
                <% }) %>
                <% if (sets.length === 0) { %>
                    <tr><td colspan="6" class="text-center">Chưa có bộ từ vựng nào.</td></tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 