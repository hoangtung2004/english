<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Quản lý Động từ bất quy tắc</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('../partials/header') %>
  <main class="container my-5">
    <h1 class="mb-4">Quản lý Động từ bất quy tắc</h1>
    <div class="mb-3 text-end">
      <a href="/admin/irregular-verbs/create" class="btn btn-success">+ Thêm động từ</a>
    </div>
    <div class="table-responsive">
      <table class="table table-bordered align-middle">
        <thead class="table-light">
          <tr>
            <th>Nguyên thể</th>
            <th>Quá khứ</th>
            <th>Quá khứ phân từ</th>
            <th>Nghĩa</th>
            <th>Thao tác</th>
          </tr>
        </thead>
        <tbody>
          <% if (verbs && verbs.length > 0) { %>
            <% verbs.forEach(verb => { %>
              <tr>
                <td><%= verb.baseForm %></td>
                <td><%= verb.pastSimple %></td>
                <td><%= verb.pastParticiple %></td>
                <td><%= verb.meaning %></td>
                <td>
                  <a href="/admin/irregular-verbs/<%= verb._id %>/edit" class="btn btn-sm btn-warning">Sửa</a>
                  <form action="/admin/irregular-verbs/<%= verb._id %>/delete" method="post" style="display:inline" onsubmit="return confirm('Xác nhận xóa động từ này?')">
                    <button type="submit" class="btn btn-sm btn-danger">Xóa</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr><td colspan="5" class="text-center">Không có động từ nào.</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </main>
  <%- include('../partials/footer') %>
</body>
</html> 