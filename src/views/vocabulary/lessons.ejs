<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" href="/images/icon.png" type="image/png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .lesson-card {
            border-radius: 16px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            margin-bottom: 24px;
            padding: 20px 28px;
            display: flex;
            align-items: center;
            background: #fff;
        }
        .lesson-img {
            width: 80px;
            height: 80px;
            object-fit: cover;
            border-radius: 12px;
            margin-right: 24px;
            background: #f6f6f6;
        }
        .lesson-info {
            flex: 1;
        }
        .lesson-title {
            font-size: 1.3rem;
            font-weight: bold;
            margin-bottom: 6px;
        }
        .lesson-desc {
            color: #555;
            margin-bottom: 8px;
        }
        .lesson-order {
            font-size: 1.1rem;
            color: #888;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Học Tiếng Anh</a>
        </div>
    </nav>
    <div class="container mt-5">
        <h1 class="mb-4 text-center">Các chủ đề trong bộ: <%= set.name %></h1>
        <div class="mb-4 text-center text-muted"><%= set.description %></div>
        <% lessons.forEach(lesson => { %>
            <div class="lesson-card">
                <% if (lesson.image) { %>
                    <img src="<%= lesson.image %>" class="lesson-img" alt="<%= lesson.name %>">
                <% } else { %>
                    <div class="lesson-img d-flex align-items-center justify-content-center text-muted">?</div>
                <% } %>
                <div class="lesson-info">
                    <div class="lesson-title"><%= lesson.name %></div>
                    <div class="lesson-desc"><%= lesson.description %></div>
                    <div class="lesson-order">Thứ tự: <%= lesson.order %></div>
                </div>
                <a href="/vocabulary/lesson/<%= lesson._id %>/flashcard" class="btn btn-success ms-3">Bắt đầu học</a>
            </div>
        <% }) %>
        <% if (lessons.length === 0) { %>
            <div class="alert alert-info text-center">Chưa có bài học nào trong bộ này.</div>
        <% } %>
        <div class="text-center mt-4">
            <a href="/vocabulary" class="btn btn-secondary">Quay lại danh sách bộ từ vựng</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 